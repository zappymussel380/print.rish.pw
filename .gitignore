# Dependencies
node_modules/
.pnpm-store/

# Build artefacts
.next/
dist/
*.tsbuildinfo

# Runtime data (uploads, PDFs) — never commit
data/
/transfer/

# Customer models and operational backups must never enter version control.
*.stl
*.3mf
*.obj
*.amf
*.pgdump
*.dump
*.tar
*.tar.gz
*.sql.gz
!packages/geometry/fixtures/*.stl
!packages/geometry/fixtures/*.3mf
!packages/geometry/fixtures/*.obj
!packages/geometry/fixtures/*.amf

# Generated Prisma client (regenerated by `prisma generate`)
packages/db/generated/

# Secrets — never commit; template only
.env
.env.*
!.env.example

# Host-specific topology/runbook (docs/DEPLOYMENT.md is the public guide)
/DEPLOYMENT.md

# OS / editor
.DS_Store
